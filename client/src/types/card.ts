export type Card = {
	
}

export type Comment = {
	comment_by: string,
	comment: string,
	_id: string
}

export type History = {
	_id: string,
	done_by: string,
	type: string,
	done_on: string,
	from: string,
	to: string,
	field: string
}