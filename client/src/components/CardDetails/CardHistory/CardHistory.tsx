import React from 'react'

type Props = {
	history: History[],
	cardId: string
}

const CardHistory: React.FC<Props> = ({ history, cardId }) => {
	return (
		<div>
      
		</div>
	)
}

export default CardHistory
